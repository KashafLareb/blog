<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles and counting!
<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>
<br><hr>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan='3'>Action</th>
    </tr>
  </thead>
  <tbody>
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.body%></td>
      <td><button><%= link_to "Show", article_path(article) %></button>
          <button><%= link_to "Edit", edit_article_path(article) %></button>
          <button><%= link_to "Destroy", article_path(article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></button>
      </td>
    </tr>
  <% end %>
  <tbody>
</table>
<hr><br>

<button><%= link_to "Create New Article", new_article_path %></button>

